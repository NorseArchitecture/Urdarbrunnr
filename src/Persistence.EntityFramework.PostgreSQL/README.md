# Norse.Persistence.EntityFramework.PostgreSQL

Canonical Aspire-wired Postgres context registration via `AddNorsePostgresContext<TContext>`. Referenced by web server and worker hosts; pulled in transitively by `Norse.Persistence.EntityFramework.Design.PostgreSQL` for the migrations service.

Part of the [Norse Architecture](https://github.com/NorseArchitecture) platform.
