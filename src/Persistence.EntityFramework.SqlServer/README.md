# Norse.Persistence.EntityFramework.SqlServer

Canonical Aspire-wired SQL Server context registration via `AddNorseSqlServerContext<TContext>`. Referenced by web server and worker hosts; pulled in transitively by `Norse.Persistence.EntityFramework.Design.SqlServer` for the migrations service.

Part of the [Norse Architecture](https://github.com/NorseArchitecture) platform.
