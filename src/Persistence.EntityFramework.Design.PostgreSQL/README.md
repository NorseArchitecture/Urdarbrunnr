# Norse.Persistence.EntityFramework.Design.PostgreSQL

Pulls in `Norse.Persistence.EntityFramework.Design` (contributor base) and `Norse.Persistence.EntityFramework.PostgreSQL` (Aspire Postgres wiring) and ships the Roslyn generator that emits `AddNorseMigrations()`. Reference this single package from your migrations service.

Part of the [Norse Architecture](https://github.com/NorseArchitecture) platform.
